Place `icon.ico` here before running the final Windows installer build.

For development builds electron-builder can run without the final icon, but release installers should include a signed icon file.
